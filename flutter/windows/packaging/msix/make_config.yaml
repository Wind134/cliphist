# MSIX config for `fastforge package --platform windows --targets msix`.
# Unsigned (sign_msix: "false"); installing an unsigned MSIX requires the
# "开发者模式" sideloading toggle. Signing can be enabled later by adding a
# certificate_path + password and flipping sign_msix to "true".
display_name: ClipHist
publisher_display_name: ping
identity_name: com.ping.cliphist
msix_version: 2.0.9.0
logo_path: assets/icon/app.png
capabilities: internetClient
languages: zh-CN
architecture: x64
sign_msix: "false"
