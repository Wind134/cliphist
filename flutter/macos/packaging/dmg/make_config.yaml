# DMG config for `fastforge package --platform macos --targets dmg`.
# appdmg-style spec; the app bundle is `my-cliphist.app` (PRODUCT_NAME from
# macos/Runner/Configs/AppInfo.xcconfig).
title: My ClipHist
icon-size: 128
contents:
  - x: 448
    y: 344
    type: link
    path: /Applications
  - x: 192
    y: 344
    type: file
    path: my-cliphist.app